/*
  Exercise:

  Let's implement a `sum(arr)` function.
  This function receives an array of numbers and
  returns the sum of them

  Example:
  ```js
  sum([2, 6, 17, 20, 1, 3])
  ```

  Should return:
  ```plain
  49
  ```
*/

/**
 * @param {Array<number>} arr
 * @returns {number}
 */
export default function sum (arr) {
  // Write your code here!
}
