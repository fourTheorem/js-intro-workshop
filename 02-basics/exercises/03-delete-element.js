/*
  Exercise:

  Let's implement a `deleteElement(arr, element)` function.
  This function receives an array of numbers and an element number
  that should be removed from the given array

  Example:

  ```js
  deleteElement([7, 9, 13, 19, 27, 49, 80], 19)
  ```

  Should return:

  ```plain
  [7, 9, 13, 27, 49, 80]
  ```

  Test your solution with:

  > npm run ex -- 02-basics/exercises/03-delete-element.test.js
*/

/**
 * @param {Array<number>} arr
 * @param {number} element
 * @returns {Array<number>}
 */
export default function deleteElement (arr, element) {
  // Write your code here!
}
